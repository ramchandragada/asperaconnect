# Easy mode (companion)

1. Build/install the APK from `apps/android`.
2. Open Aspera Connect on the phone → **Start mirroring** (grant capture).
3. Enable accessibility + notification access from the app buttons.
4. On Linux: **Easy mode** tab → enter phone IP → **Register session**.

Traffic stays on your LAN. Set a PIN on both sides for a simple shared secret.

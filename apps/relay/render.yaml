services:
  - type: web
    name: aspera-relay
    runtime: node
    plan: free
    rootDir: apps/relay
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: SESSION_TTL_MS
        value: "600000"
